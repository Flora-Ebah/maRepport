fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9   # upload de fichiers (import)
openpyxl>=3.1             # lecture/écriture Excel (import & export)
PyPDF2>=3.0               # lecture des PDF (balance)
reportlab>=4.0            # génération PDF (export)
psycopg2-binary>=2.9      # PostgreSQL (optionnel ; SQLite par défaut)
